LeftSideBar.jsx — Line-by-line, token-level explanation (for absolute beginners)

File: frontend/src/components/LeftSideBar.jsx
Goal: Explain each line and punctuation so a beginner understands how the left navigation is built and how links and active states work.

--- Top imports ---
1) import React from 'react'
- Bring React into scope so JSX works and components can be declared.

2) import { Link, useLocation } from 'react-router-dom'
- Link: component used to navigate without full page reloads.
- useLocation: hook that returns current URL location; used to show which nav item is active.

3) import { Home, Search, MessageSquare, User, PlusCircle } from 'lucide-react'
- Import five icon components from the lucide icon set.

--- Component ---
4) const LeftSideBar = () => {
- Define a functional component named LeftSideBar.

5) const location = useLocation()
- Get current route (an object with pathname) so we can compare it to nav links.

6) const nav = [
    { to: '/', label: 'Home', icon: <Home size={20} /> },
    { to: '/explore', label: 'Explore', icon: <Search size={20} /> },
    { to: '/messages', label: 'Messages', icon: <MessageSquare size={20} /> },
    { to: '/create', label: 'Create', icon: <PlusCircle size={20} /> },
    { to: '/profile', label: 'Profile', icon: <User size={20} /> },
]
- nav is an array of objects. Each object describes one item: the `to` path, the `label` text, and an `icon` JSX node.

7) return (
    <aside className='hidden md:flex flex-col gap-4 p-4 sticky top-0'>
- Render an <aside> element used for sidebar content. Tailwind classes hide it on small screens and make it sticky on larger screens.

8) {nav.map(item => (
    <Link key={item.to} to={item.to} className={`flex items-center gap-3 p-2 rounded-md ${location.pathname === item.to ? 'bg-gray-200' : 'hover:bg-gray-100'}`}>
        <div>{item.icon}</div>
        <span className='text-sm'>{item.label}</span>
    </Link>
))}
- For each nav item, render a <Link>. Use `location.pathname === item.to` to conditionally add an active background class.
- `key={item.to}` gives React a stable identity for each list item.

9) </aside>
- Close the <aside>.

10) }
- Close the component function.

11) export default LeftSideBar
- Make the component available to other files.

--- Beginner summary ---
- LeftSideBar renders a vertical list of navigation links with icons. The current route gets a different background so the user knows which page is active.

End of LeftSideBar line-by-line doc.